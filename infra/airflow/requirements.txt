# Airflow worker dependencies for hallucin8 DAGs
# These are installed on top of the base apache/airflow image.
# Keep in sync with pyproject.toml [project.dependencies].

openai>=1.30.0
qdrant-client>=1.9.0
psycopg2-binary>=2.9.0
redis>=5.0.0
confluent-kafka>=2.4.0
structlog>=24.2.0
pydantic>=2.7.0
numpy>=1.26.0
tenacity>=8.3.0
neo4j>=5.0.0
vaderSentiment>=3.3.2
google-generativeai>=0.7.0
